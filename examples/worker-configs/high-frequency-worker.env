# High-Frequency Monitoring Worker Configuration
# For real-time monitoring and training workloads

# all-smi version to use
ALL_SMI_VERSION=v0.9.0

# Port for metrics endpoint
ALL_SMI_PORT=9090

# High-frequency collection (1 second interval)
ALL_SMI_INTERVAL=1

# Bind address for external access
HOST_IP=0.0.0.0

# Custom hostname for identification
HOSTNAME=gpu-worker-realtime

# Labels for high-frequency worker
WORKER_LABELS=monitoring_type=realtime,frequency=high,purpose=training