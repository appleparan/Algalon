# Production Algalon Worker Configuration
# Optimized for production environments with security considerations

# all-smi version to use (pinned for stability)
ALL_SMI_VERSION=v0.9.0

# Port for metrics endpoint
ALL_SMI_PORT=9090

# Standard collection interval (balanced performance/overhead)
ALL_SMI_INTERVAL=5

# Bind to specific interface for security
# Change to 0.0.0.0 if external access is needed
HOST_IP=0.0.0.0

# Production hostname
HOSTNAME=gpu-prod-worker-$(hostname)

# Production labels
WORKER_LABELS=environment=production,tier=compute,monitoring=algalon

# Optional: Resource limits (uncomment if needed)
# MEMORY_LIMIT=512m
# CPU_LIMIT=0.5

# Optional: Log configuration
# LOG_LEVEL=info
# LOG_FILE=/var/log/algalon-worker.log