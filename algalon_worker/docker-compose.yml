services:
  all-smi:
    build:
      context: https://github.com/inureyes/all-smi.git
      dockerfile: Dockerfile
    container_name: algalon-all-smi
    ports:
      - "9090:9090"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    runtime: nvidia
    cap_add:
      - SYS_ADMIN
    command: ["api", "--port", "9090", "--interval", "5", "--processes"]
    restart: unless-stopped
    networks:
      - monitoring

networks:
  monitoring:
    driver: bridge
