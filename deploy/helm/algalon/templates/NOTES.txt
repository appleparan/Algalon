Algalon {{ .Chart.AppVersion }} has been installed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.
{{- if .Values.host.enabled }} The host stack exposes Grafana on port 3000 (login {{ .Values.grafana.adminUser }} / the configured admin password), VictoriaMetrics on 8428, vmalert on 8880 and Alertmanager on 9093 as in-cluster ClusterIP Services named after {{ include "algalon.fullname" . }}; reach them with e.g. `kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "algalon.fullname" . }}-grafana 3000:3000`. Slack notifications stay disabled until a Secret supplies the webhook URLs: either set `alertmanager.slack.existingSecret` to a Secret holding the keys `slack_webhook_critical` and `slack_webhook_warning`, or set `alertmanager.slack.criticalUrl` / `alertmanager.slack.warningUrl` so the chart creates that Secret for you (avoid the latter under GitOps, since values files are committed to git).
{{- else }} Only the worker exporters were installed (host.enabled=false); point an existing Algalon host stack at the DCGM exporter on port {{ .Values.dcgmExporter.port }} and node-exporter on port {{ .Values.nodeExporter.port }}.
{{- end }}
